#include "main.h"
#include <string.h>

/**
 * _strcpy - copies string pointed
 * @dest: first argument
 * @src: second argument
 *
 * Return: Char
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
