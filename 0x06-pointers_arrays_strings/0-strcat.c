#include "main.h"

/**
 * _strcat - function that concatenates two strings
 * @dest: first argument
 * @src: second argument
 *
 * Return: char
 */

char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
